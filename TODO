Export/import:
Menu items are too easy to hit by accident, move export/import to their own activities?

Other:
add some statistics
replace hardcoded strings with values in strings.xml
handle database upgrades

Preferences:
make preferences general for all users
confirmation dialog for reset counters
add sensitivity for shake-to-clear
